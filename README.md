# Intelligent Candidate Discovery

Production-ready AI pipeline for ranking candidates against a job description using schema-aware parsing, semantic embeddings, explainable scoring, and submission generation.

This repository is currently scaffolded to support the full hackathon workflow. The dataset files referenced in the brief are not present in the workspace yet, so the code is designed to inspect them once they are added under `data/raw/`.